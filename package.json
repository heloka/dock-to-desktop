{
  "name": "obsidian-dock-to-desktop",
  "version": "2.0.2",
  "private": true,
  "description": "Windows AppBar based quick-note window for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "test": "tsx --test tests/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "koffi": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
